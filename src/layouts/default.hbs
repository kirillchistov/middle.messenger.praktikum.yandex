<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App{{#if title}} | {{title}}{{/if}}</title>
    
    <!-- Глобальные стили -->
    <link rel="stylesheet" href="/styles/globals.css">
    <link rel="stylesheet" href="/styles/components/button.css">
    <link rel="stylesheet" href="/styles/components/input.css">
    
    <!-- Блок для дополнительных стилей страниц -->
    {{#if styles}}
        {{#each styles}}
            <link rel="stylesheet" href="{{this}}">
        {{/each}}
    {{/if}}
    
    <!-- Иконка -->
    <link rel="icon" type="image/svg+xml" href="/assets/logo.svg">
</head>
<body>
    {{#if showHeader}}
    <header class="header">
        <div class="container">
            <nav class="nav">
                <a href="/" class="logo">
                    <img src="/assets/logo.svg" alt="Chat App YP" width="40" height="40">
                    <span>Chat App</span>
                </a>
                {{#if user}}
                <div class="nav-user">
                    <a href="/profile" class="user-link">
                        <img src="{{user.avatar}}" alt="{{user.name}}" class="user-avatar" width="32" height="32">
                        <span>{{user.name}}</span>
                    </a>
                    <a href="/logout" class="button button--small button--secondary">Выйти</a>
                </div>
                {{/if}}
            </nav>
        </div>
    </header>
    {{/if}}

    <main class="main">
        {{{body}}}
    </main>

    {{#if showFooter}}
    <footer class="footer">
        <div class="container">
            <p>© 2025 Chat App YP. Some rights reserved.</p>
        </div>
    </footer>
    {{/if}}

    <!-- Блок для дополнительных скриптов страниц -->
    {{#if scripts}}
        {{#each scripts}}
            <script src="{{this}}" defer></script>
        {{/each}}
    {{/if}}
</body>
</html>